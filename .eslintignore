# ESLint ignore file - Files and directories to skip during linting
# This helps maintain a clean separation between code quality checks
# and files that are not part of the core linting process

# Build outputs
dist/
bin/
coverage/

# Dependencies
node_modules/

# Test files temporarily excluded during code quality improvement
**/*.test.ts
src/verification/tests/**/*
tests/**/*.broken

# Terminal bridge (has specific configuration issues)
src/terminal/**/*

# Specific files with complex issues
src/cli/commands/swarm-new.ts